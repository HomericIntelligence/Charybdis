{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "schedule": ["before 5am on Monday"],
  "packageRules": [
    {
      "matchManagers": ["conan"],
      "groupName": "C++ Conan dependencies"
    },
    {
      "description": "Python build-toolchain wheels (incl. conan in pyproject.toml + uv.lock) are owned by Dependabot's fleet-baseline 'uv' ecosystem (.github/dependabot.yml); Renovate must not open duplicate update PRs.",
      "matchManagers": ["pep621", "uv"],
      "enabled": false
    },
    {
      "description": "GitHub Actions bumps are owned by Dependabot's fleet-baseline 'github-actions' ecosystem (.github/dependabot.yml); Renovate must not open duplicate update PRs.",
      "matchManagers": ["github-actions"],
      "enabled": false
    }
  ]
}
